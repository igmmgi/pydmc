from setuptools import setup

setup(name='dmc',
      version='1.0.1',
      description='Diffusion Model of Conflict (DMC)',
      author='IGM',
      packages=['dmc'],
      license='MIT',
      zip_safe=False)
